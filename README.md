# FFX-Downgrader
A lightweight Python tool that downgrades Adobe After Effects .ffx preset files to older AE versions by modifying the internal version byte in the preset’s RIFX container. This allows presets created in newer versions of After Effects to be used in older releases when compatible.
